{
  "name": "poetry-web-ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "bunx --bun vite",
    "build": "rm -rf ../docs && bunx --bun vite build && touch ../docs/.nojekyll",
    "preview": "bunx --bun vite preview"
  },
  "devDependencies": {
    "typescript": "~5.7.2",
    "vite": "^6.2.0"
  },
  "dependencies": {
    "seedrandom": "^3.0.5"
  }
}